export const apiVersion =
    process.env.NEXT_PUBLIC_SANITY_API_VERSION || '2026-01-21'

// Hardcoded values for Sanity Studio deployment
// These are public values (not secrets) so it's safe to include them
export const dataset = 'production'

export const projectId = '4wgl7wc5'
